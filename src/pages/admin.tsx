import type { NextPage } from 'next';

const Admin: NextPage = () => {
    console.log('Admin: ', Admin);
    return (
        <div>
            <p>Admin page</p>
        </div>
    );
};

export default Admin;
